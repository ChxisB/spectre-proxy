# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql
version: 2

project_name: spectre

includes: []

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"
  license: "FSL-1.1-MIT"
  homepage: "https://github.com/ChxisB/spectre-proxy"
  description: "A powerful terminal-based AI assistant for developers, providing intelligent coding assistance directly in your terminal."
  full_description: "A powerful terminal-based AI assistant for developers, providing intelligent coding assistance directly in your terminal."
  maintainers:
    - "Spectre Proxy Team"
    - "Spectre Proxy Team" # for aur

nightly:
  publish_release: true
  keep_single_release: true
  version_template: "{{ incminor .Version }}-nightly"

snapshot:
  version_template: "0.0.0-{{ .Timestamp }}"

before:
  hooks:
    - go mod tidy
    - rm -rf completions
    - mkdir completions
    - rm -rf manpages
    - mkdir manpages
    - sh -c 'go run . completion bash >./completions/spectre.bash'
    - sh -c 'go run . completion zsh >./completions/spectre.zsh'
    - sh -c 'go run . completion fish >./completions/spectre.fish'
    - sh -c 'go run . man | gzip -c >./manpages/spectre.1.gz'

# gomod:
#   proxy: true

builds:
  - env:
      - CGO_ENABLED=0
      - GOEXPERIMENT=greenteagc
    goos:
      - linux
      - darwin
      - windows
      - freebsd
      - openbsd
      - netbsd
      - android
    goarch:
      - amd64
      - arm64
      - "386"
      - arm
    goarm:
      - "7"
    ignore:
      - goos: android
        goarch: amd64
      - goos: android
        goarch: arm
      - goos: android
        goarch: "386"
      - goos: windows
        goarch: arm
    ldflags:
      - -s -w -X github.com/ChxisB/spectre-proxy/internal/version.Version={{.Version}}
    flags:
      - -trimpath

archives:
  - name_template: >-
      spectre_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- with .Arm}}v{{ . }}{{ end }}
    wrap_in_directory: true
    files:
      - README*
      - LICENSE*
      - manpages/*
      - completions/*
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

aur_sources:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/spectre.git"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"
    makedepends:
      - go
      - git
    prepare: |-
      go mod download
    build: |-
      mkdir completions
      mkdir manpages
      export CGO_CPPFLAGS="${CPPFLAGS}"
      export CGO_CFLAGS="${CFLAGS}"
      export CGO_CXXFLAGS="${CXXFLAGS}"
      export CGO_LDFLAGS="${LDFLAGS}"
      export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
      go build -ldflags="-w -s -buildid='' -linkmode=external -X github.com/ChxisB/spectre-proxy/internal/version.Version=v${pkgver}" .
      ./spectre completion bash >./completions/spectre.bash
      ./spectre completion zsh >./completions/spectre.zsh
      ./spectre completion fish >./completions/spectre.fish
      ./spectre man | gzip -c >./manpages/spectre.1.gz
    package: |-
      # bin
      install -Dm755 "./spectre" "${pkgdir}/usr/bin/spectre"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/spectre/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/spectre/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/spectre.bash" "${pkgdir}/usr/share/bash-completion/completions/spectre"
      install -Dm644 "./completions/spectre.zsh" "${pkgdir}/usr/share/zsh/site-functions/_spectre"
      install -Dm644 "./completions/spectre.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/spectre.fish"
      # man pages
      install -Dm644 "./manpages/spectre.1.gz" "${pkgdir}/usr/share/man/man1/spectre.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/spectre/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/spectre/"

aurs:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/spectre-bin.git"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"
    provides:
      - spectre
    conflicts:
      - spectre
    package: |-
      case "$CARCH" in
        aarch64)
          cd "${srcdir}/spectre_${pkgver}_Linux_arm64"
          ;;
        *)
          cd "${srcdir}/spectre_${pkgver}_Linux_${CARCH}"
          ;;
      esac
      # bin
      install -Dm755 "./spectre" "${pkgdir}/usr/bin/spectre"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/spectre/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/spectre/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/spectre.bash" "${pkgdir}/usr/share/bash-completion/completions/spectre"
      install -Dm644 "./completions/spectre.zsh" "${pkgdir}/usr/share/zsh/site-functions/_spectre"
      install -Dm644 "./completions/spectre.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/spectre.fish"
      # man pages
      install -Dm644 "./manpages/spectre.1.gz" "${pkgdir}/usr/share/man/man1/spectre.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/spectre/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/spectre/"

furies:
  - disable: "{{ if (or .Prerelease .IsNightly) }}true{{ end }}"
    account: "{{ with .Env.FURY_TOKEN }}spectre-proxy{{ else }}{{ end }}"
    secret_name: FURY_TOKEN

brews:
  - repository:
      owner: ChxisB
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"
    goarm: 7
    extra_install: |-
      bash_completion.install "completions/{{ .ProjectName }}.bash" => "{{ .ProjectName }}"
      zsh_completion.install "completions/{{ .ProjectName }}.zsh" => "_{{ .ProjectName }}"
      fish_completion.install "completions/{{ .ProjectName }}.fish"
      man1.install "manpages/{{ .ProjectName }}.1.gz"

scoops:
  - repository:
      owner: ChxisB
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"

npms:
  - name: "spectre-proxy"
    repository: "git+https://github.com/ChxisB/spectre-proxy.git"
    bugs: https://github.com/ChxisB/spectre-proxy/issues
    access: public
    disable: "{{ with .Prerelease }}true{{ end }}"

nfpms:
  - formats:
      - apk
      - deb
      - rpm
      - archlinux
      - termux.deb
    file_name_template: "{{ .ConventionalFileName }}"
    contents:
      - src: ./completions/spectre.bash
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files/usr{{ end }}/etc/bash_completion.d/spectre'
      - src: ./completions/spectre.fish
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/fish/vendor_completions.d/spectre.fish'
      - src: ./completions/spectre.zsh
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/zsh/site-functions/_spectre'
      - src: ./manpages/spectre.1.gz
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/man/man1/spectre.1.gz'
    rpm:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'
    deb:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

source:
  enabled: true

sboms:
  - artifacts: archive
  - id: source
    artifacts: source

nix:
  - repository:
      owner: "ChxisB"
      name: nur
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"
    license: fsl11Mit
    formatter: nixfmt
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    extra_install: |-
      installManPage ./manpages/spectre.1.gz
      installShellCompletion ./completions/*

winget:
  - publisher: ChxisB
    copyright: Spectre Proxy
    publisher_url: https://github.com/ChxisB/spectre-proxy
    release_notes_url: "https://github.com/ChxisB/spectre-proxy/releases/tag/{{.Tag}}"
    license_url: https://github.com/ChxisB/spectre-proxy/blob/main/LICENSE.md
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "Spectre Proxy"
      email: "spectre-proxy@users.noreply.github.com"
    repository:
      owner: "ChxisB"
      name: winget-pkgs
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
      branch: "spectre-{{.Version}}"
      pull_request:
        enabled: true
        draft: false
        check_boxes: true
        base:
          owner: microsoft
          name: winget-pkgs
          branch: master
        body: |
          /cc @andreynering

changelog:
  sort: asc
  disable: "{{ .IsNightly }}"
  use: github
  filters:
    exclude:
      - "^(build|ci): "
      - "^chore: auto-update generated files$"
      - "^chore: docs$"
      - "^chore: schema update$"
      - "^chore: schema$"
      - "^chore: typo$"
      - "^chore: update schema$"
      - "^chore: update$"
      - "^chore\\(legal\\): "
      - "^chore\\(deps\\): "
      - "^docs: update$"
      - "^test:"
      - "^test\\("
      - "^v\\d.*"
      - "merge conflict"
      - "merge conflict"
      - Merge branch
      - Merge pull request
      - Merge remote-tracking branch
      - go mod tidy
      - "^wip "
      - "^wip:"
  groups:
    - title: "Deps"
      regexp: "^.*\\(deps\\)*:+.*$"
      order: 300
    - title: "New!"
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 100
    - title: "Fixed"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 200
    - title: "Docs"
      regexp: "^.*docs[(\\w)]*:+.*$"
      order: 400
    - title: "Other stuff"
      order: 9999

release:
  prerelease: auto
  footer:
    from_url:
      url: https://raw.githubusercontent.com/ChxisB/spectre-proxy/main/CHANGELOG_FOOTER.md
